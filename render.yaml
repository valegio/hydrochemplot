services:
  - type: web
    name: hydrochemplot
    plan: free
    buildCommand: |
      python -m pip install --upgrade pip
      pip install --no-cache-dir --only-binary numpy,pandas,matplotlib -r requirements.txt
      mkdir -p static/output
    startCommand: gunicorn --workers 1 --threads 1 --timeout 120 app:app
